name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  placeholder:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add xcodebuild tests after project bootstrap"
